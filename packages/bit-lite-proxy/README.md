# bit-lite-proxy

Transport-only HTTP/WebSocket routing for bit-lite development commands.

The package owns listening lifecycle, ordered route dispatch, response helpers,
and reverse proxying. Preview, test, command, manifest, and UI state stay in
their owning packages.
